<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="30dp" /> <!-- Rayon des coins arrondis -->
    <solid android:color="@android:color/transparent" /> <!-- Couleur de fond du rectangle -->
    <size
        android:width="20dp"
        android:height="20dp" />
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /><!-- Hauteur du rectangle -->
</shape>
